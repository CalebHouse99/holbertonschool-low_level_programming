#include <stdio.h>
#include "main.h"
/**
 * _strcat - dest combines with src
 * @dest: first string
 * @src: second string
 * Return: 0;
 */
char *_strcat(char *dest, char *src)
{
_strcat(dest, src);
return (0);
}
