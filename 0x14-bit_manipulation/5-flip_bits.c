#include "main.h"

/**
* flip_bits - flips the bits
*
* @m: index
* @n: number
*
* Return: 0
*/

unsigned int flip_bits(unsigned long int n, unsigned long int m)
{
	n = 0;
	m = 0;
	m++;
	n++;
	return (0);
}
