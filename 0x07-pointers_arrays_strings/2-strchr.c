#include "main.h"
/**
 * _strchr - replaces characters
 *@s: first occurrence
 *@c: of this
 * Return: first character
 */
char *_strchr(char *s, char c);
{
unsigned int a;
for (a = 0; a == c; a++)
{
c[s] = c[a];
}
return (s);
}
