#include "main.h"
/**
 * _memset - replaces characters
 *@dest: destination
 *@b: filler
 *@n: number of bytes
 * Return: 0
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
unsigned int a;
for (a = 0; a < n; a++)
{
dest[a] = src[a];
}
return (s);
}
